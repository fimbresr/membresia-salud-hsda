name: Deploy a GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependencias
        run: cd frontend && npm install

      - name: Build
        run: cd frontend && npm run build

      - name: Copiar service worker y manifest al build
        run: |
          cp frontend/public/sw.js frontend/dist/
          cp frontend/public/manifest.json frontend/dist/
          cp frontend/public/icon-192x192.png frontend/dist/
          cp frontend/public/icon-512x512.png frontend/dist/

      - name: Deploy a GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: frontend/dist
          force_orphan: true
